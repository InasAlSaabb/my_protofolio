# 🔄 كيفية نقل التعديلات من main إلى gh-pages

إذا كنت تعمل على `gh-pages` مباشرة، فالتعديلات في `main` لن تظهر. إليك الحل:

## ✅ الحل الموصى به: العمل على main ثم النشر التلقائي

### الخطوات:

#### 1. رفع التعديلات إلى main:

```powershell
# التأكد من أنك في branch main
git checkout main

# إضافة التعديلات
git add .

# عمل commit
git commit -m "Update portfolio - $(Get-Date -Format 'yyyy-MM-dd')"

# رفع إلى main
git push origin main
```

#### 2. GitHub Actions سينشر تلقائياً:

بعد رفع التعديلات على `main`، GitHub Actions سيقوم بـ:
- ✅ بناء المشروع تلقائياً
- ✅ رفع ملفات `build/web` على `gh-pages`
- ✅ تحديث "Last deployed" تلقائياً

**انتظر 1-2 دقيقة** وستجد التعديلات على الموقع!

---

## 🔄 إذا كان لديك تعديلات في gh-pages وتريد نقلها لـ main:

### الطريقة 1: نسخ الملفات يدوياً

```powershell
# 1. بناء المشروع من التعديلات الحالية
flutter build web --base-href "/my_protofolio/"

# 2. التأكد من أنك في main
git checkout main

# 3. إضافة جميع التعديلات
git add .

# 4. Commit و push
git commit -m "Sync changes from gh-pages to main"
git push origin main
```

### الطريقة 2: دمج branches

```powershell
# 1. التأكد من أنك في main
git checkout main

# 2. جلب آخر التعديلات
git pull origin main

# 3. دمج gh-pages في main (إذا كان gh-pages يحتوي على كود مصدر)
git merge gh-pages

# 4. حل أي تعارضات إذا وجدت
# 5. رفع
git push origin main
```

---

## 🚀 الطريقة الصحيحة للعمل (موصى به):

### 1. اعمل دائماً على main:

```powershell
# التأكد من أنك في main
git checkout main

# عمل التعديلات في الكود
# ... تعديل الملفات ...

# بناء واختبار محلياً
flutter run -d chrome

# إذا كل شيء تمام، ارفع
git add .
git commit -m "Update: وصف التعديلات"
git push origin main
```

### 2. GitHub Actions سينشر تلقائياً:

- ✅ لا حاجة للذهاب إلى `build/web`
- ✅ لا حاجة لرفع يدوي على `gh-pages`
- ✅ كل شيء تلقائي!

---

## 📝 ملاحظات مهمة:

1. **main** = الكود المصدر (lib/, web/, pubspec.yaml, إلخ)
2. **gh-pages** = ملفات build/web فقط (يتم إنشاؤها تلقائياً)
3. **لا تعدل gh-pages يدوياً** - اتركه لـ GitHub Actions
4. **اعمل دائماً على main** - هذا هو الكود المصدر

---

## 🔍 التحقق من أن كل شيء يعمل:

### 1. تحقق من main:
```powershell
git checkout main
git log --oneline -5
```
يجب أن ترى آخر commits مع التعديلات.

### 2. تحقق من GitHub Actions:
- اذهب إلى: `https://github.com/USERNAME/REPO_NAME/actions`
- يجب أن ترى workflow "Deploy to GitHub Pages" يعمل

### 3. تحقق من gh-pages:
- اذهب إلى: `https://github.com/USERNAME/REPO_NAME/tree/gh-pages`
- يجب أن ترى فقط ملفات `build/web`

---

## ❓ إذا لم يعمل النشر التلقائي:

### 1. تحقق من أن GitHub Actions مفعّل:
- Settings → Actions → General
- تأكد من أن "Allow all actions and reusable workflows" مفعّل

### 2. تحقق من أن workflow موجود:
- يجب أن يكون `.github/workflows/deploy.yml` موجود في main

### 3. شغّل workflow يدوياً:
- اذهب إلى Actions tab
- اختر "Deploy to GitHub Pages"
- اضغط "Run workflow"

---

## ✅ الخلاصة:

**الطريقة الصحيحة:**
1. ✅ اعمل على `main` (الكود المصدر)
2. ✅ ارفع التعديلات على `main`
3. ✅ GitHub Actions سينشر تلقائياً على `gh-pages`
4. ✅ الموقع سيُحدّث تلقائياً

**لا تفعل:**
- ❌ لا تعمل على `gh-pages` مباشرة
- ❌ لا ترفع ملفات المصدر على `gh-pages`
- ❌ لا تعدل `build/web` يدوياً

---

**جاهز! 🎉**
